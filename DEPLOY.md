# 部署说明

## 项目特点

本项目是纯前端 SPA（单页应用），可以直接部署到任何静态网站托管平台。

- **前端**：原生 HTML + Tailwind CSS + JavaScript
- **后端**：可选的 Node.js Express 服务（用于数据持久化和 WebSocket）
- **降级机制**：没有后端时自动降级为本地内存模式，所有功能仍可正常使用（数据刷新后重置）

## 部署到 GitHub Pages

1. 将整个项目上传到 GitHub 仓库
2. 仓库设置 → Pages → Source 选择 `main` 分支根目录
3. 等待部署完成，访问 `https://你的用户名.github.io/仓库名/`

**注意**：
- 项目已包含 `.nojekyll` 文件，防止 Jekyll 处理
- 纯静态模式下，数据存储在浏览器内存中，刷新页面后重置
- 如果需要数据持久化，需要部署后端服务

## 部署到 Vercel

1. 导入 GitHub 仓库到 Vercel
2. 项目已包含 `vercel.json` 配置，会自动识别为静态网站
3. 点击 Deploy 即可

## 部署到 Netlify

1. 导入 GitHub 仓库到 Netlify
2. 项目已包含 `netlify.toml` 配置，会自动识别为静态网站
3. 点击 Deploy 即可

## 部署到云服务器（带后端）

如果需要数据持久化和 WebSocket 实时更新功能，需要部署后端服务：

1. 确保服务器已安装 Node.js 16+
2. 上传项目文件到服务器
3. 安装依赖：`npm install`
4. 启动服务：`npm start`
5. 配置 Nginx 反向代理到 3000 端口

## 常见问题

### 页面空白/打不开

1. **检查文件是否完整上传**：确保 `assets` 目录和所有文件都上传了
2. **检查大小写**：Linux 服务器区分大小写，确保文件名大小写一致
3. **检查浏览器控制台**：按 F12 查看 Console 面板的错误信息
4. **CSP 限制**：如果平台有严格的内容安全策略，Tailwind JS 版本可能无法工作，建议换成预编译的 CSS 版本

### 样式不对/图标不显示

1. 检查 `assets/lib` 目录下的文件是否完整上传
2. 检查文件路径是否正确
3. 刷新页面清除缓存

### 数据刷新后丢失

纯静态模式下，数据存储在浏览器内存中，刷新页面后会重置。这是正常现象。

如果需要数据持久化，请部署后端服务。
